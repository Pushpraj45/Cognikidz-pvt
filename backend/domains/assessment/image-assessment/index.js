// Image-Based Assessment Subdomain - Main exports
const controller = require("./controller");
const service = require("./service");
const model = require("./model");
const routes = require("./routes");

module.exports = {
  controller,
  service,
  model,
  routes,
};
